machine:
  php:
    version: 5.5.11

test:
  override:
    - phpunit

deployment:
  production:
    branch: production
    commands:
      - ./deploy_prod.sh
  staging:
    branch: master
    commands:
      - echo hoge
      - ./deploy_staging.sh
