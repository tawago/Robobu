machine:
  php:
    version: 5.5.11

test:
  override:
    - ./deploy.sh

deployment:
  production:
    branch: production
    commands:
      - ./deploy.sh
  staging:
    branch: master
    commands:
      - echo OK
      - ./deploy.sh
